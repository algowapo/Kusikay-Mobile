import 'package:flutter/material.dart';

class MyReportsLeader extends StatelessWidget {
  const MyReportsLeader({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('hola mis reportes'),
    );
  }
}
