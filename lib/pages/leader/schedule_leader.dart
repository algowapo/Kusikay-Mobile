import 'package:flutter/material.dart';

class ScheduleLeader extends StatelessWidget {
  const ScheduleLeader({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('hola mi horario'),
    );
  }
}
