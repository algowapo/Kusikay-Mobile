import 'package:flutter/material.dart';

class ReviewReports extends StatelessWidget {
  const ReviewReports({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('hola revisar reportes'),
    );
  }
}
