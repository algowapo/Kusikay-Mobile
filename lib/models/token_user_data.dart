class TokenUserData {
  int id;
  String token;
  String username;
  String password;
  String role;
  bool enabled;
  TokenUserData(this.id, this.token, this.username, this.password, this.role,
      this.enabled);
}
